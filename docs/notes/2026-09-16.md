# Working notes: 2026-09-16

- Seed `20260916` produced mode roll `0.6624941903573036`, selecting new-project mode.
- The previous four completed scheduled runs covered Android, software engineering, games, and iOS, so data science was the sole eligible focused lane.
- Local folders and the visible public GitHub inventory were reviewed. Existing data work covers forecasting, churn, dataset quality, model benchmarking, predictive maintenance, ML operations, and decision-rank uncertainty. No project audits aggregate-versus-stratified direction reversals.
- The project was scoped to binary treatment and outcome data so validation, calculations, evidence, and interpretation boundaries could all be completed in one run.
- Pooled comparable-stratum shares were selected as a symmetric, inspectable standardization target.
- The first implementation and 11-test suite passed. The 2,000-row demo produced the designed -20 percentage point aggregate effect and +5 percentage point standardized effect.
- The output is intentionally descriptive. Causal language is rejected in the product copy and documentation.
- GitHub created the public repository, but the saved OAuth credential rejected a push containing `.github/workflows/ci.yml` because it lacks `workflow` scope. SSH authentication was also unavailable. The unpushed root commit was amended to exclude the workflow, and all hosted-CI claims were removed. Adding the intended Python 3.10/3.12/3.14 workflow requires granting that one scope or committing the workflow through an authorized GitHub session.
